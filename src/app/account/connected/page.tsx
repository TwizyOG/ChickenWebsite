import AccountConnected from "@/components/account/AccountConnected";

export const metadata = { title: "Connected Accounts — ChickenAndy" };

export default function ConnectedPage() {
  return <AccountConnected />;
}
