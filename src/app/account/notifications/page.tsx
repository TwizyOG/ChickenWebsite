import AccountNotifications from "@/components/account/AccountNotifications";

export const metadata = { title: "Notifications — ChickenAndy" };

export default function NotificationsPage() {
  return <AccountNotifications />;
}
