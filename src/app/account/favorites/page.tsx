import AccountFavorites from "@/components/account/AccountFavorites";

export const metadata = { title: "Favorite Streamers — ChickenAndy" };

export default function FavoritesPage() {
  return <AccountFavorites />;
}
