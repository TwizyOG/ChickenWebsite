import AccountSecurity from "@/components/account/AccountSecurity";

export const metadata = { title: "Security — ChickenAndy" };

export default function SecurityPage() {
  return <AccountSecurity />;
}
