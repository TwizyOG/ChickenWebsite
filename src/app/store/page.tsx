import PagePlaceholder from "@/components/PagePlaceholder";

export const metadata = { title: "Store — ChickenAndy" };

export default function StorePage() {
  return (
    <PagePlaceholder eyebrow="Store" title="Store">
      <p>Merch drops and crew collabs are coming soon. Check back before the next leg of the trip.</p>
    </PagePlaceholder>
  );
}
